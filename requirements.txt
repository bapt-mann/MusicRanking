# Windows uniquement (capture SMTC) - wheels installes automatiquement :
winrt-Windows.Media.Control; sys_platform == "win32"
winrt-Windows.Foundation; sys_platform == "win32"
winrt-Windows.Foundation.Collections; sys_platform == "win32"

# Linux : PAS de paquet pip. Installer playerctl via le gestionnaire systeme :
#   Debian/Ubuntu : sudo apt install playerctl
#   Fedora        : sudo dnf install playerctl
#   Arch          : sudo pacman -S playerctl
